diff --git a/src/DaggerfallEdit.Cli/Program.cs b/src/DaggerfallEdit.Cli/Program.cs
--- a/src/DaggerfallEdit.Cli/Program.cs
+++ b/src/DaggerfallEdit.Cli/Program.cs
@@ -882,6 +882,23 @@ static string NormalizeLooseJsonForLenientParsing(string text)
             continue;
         }
 
+        if (c == '}')
+        {
+            builder.Append(c);
+
+            // Some loose JSON-like data files contain adjacent objects inside an
+            // array without the required comma between them:
+            //     }
+            //     {
+            // Normalize this case while preserving the original whitespace so the
+            // parser can continue and the JSON paths still correspond to object
+            // order in the source file.
+            int next = i + 1;
+            while (next < text.Length && char.IsWhiteSpace(text[next]))
+                next++;
+
+            if (next < text.Length && text[next] == '{')
+                builder.Append(',');
+
+            continue;
+        }
+
         if (c == '-' && i + 2 < text.Length && text[i + 1] == '0' && char.IsDigit(text[i + 2]) && IsLooseJsonNumberTokenStart(text, i))
         {
             int digitStart = i + 1;
